	 <p>
	  Wake Up Manager offers the possibilty to boot and shutdown hosts based on a timetable.
	 </p>

	<!-- Handle errors first //-->
	<TMPL_IF error>
	 <p class="error">

	 <TMPL_IF no_host_id>
	   Error: Parameter 'host_id' missing.
	 </TMPL_IF>

	 <TMPL_IF user_not_allowed>
	   You are not allowed to update the timetable of host <TMPL_VAR hostname>.
	 </TMPL_IF>

	 <TMPL_IF cant_read_timetable>
	  Error: Could not load timetable of host <TMPL_VAR hostname>.
	 </TMPL_IF>

	 </p>
	<TMPL_ELSE>


	<!-- If the user has the right to read the config of least on host //-->
	<TMPL_IF host_loop>
	 <form name="show_config_form" action="index.pl" method="GET">
	  <input type="hidden" name="page" value="UpdateTimetable">
	  <input type="hidden" name="submitted" value="0">

	  <p>
	   Choose the host, which timetable should be shown and updated.
	  </p>

	  <div class="box">
	   <div class="box_head">&raquo; Show timetable</div>
	   <div class="box_content">

	    <div id='host_select'>
	     <select name="host_id">
	      <TMPL_LOOP NAME="host_loop"><option value="<TMPL_VAR NAME=key>"><TMPL_VAR NAME=val></option>
	      </TMPL_LOOP>
	     </select>
	    </div>

	    <!-- Nur Rechner der Gruppe <Hostgroup> anzeigen. //-->
	    <TMPL_IF hostgroup_loop>
	     <p class='hostgroup_list'>
	      Only show hosts of group

	      <select name="group" onChange="updateHostlist();">
	       <TMPL_LOOP NAME="hostgroup_loop"><option value="<TMPL_VAR NAME=key>"><TMPL_VAR NAME=val></option>
	       </TMPL_LOOP>
	      </select>
	     </p>
	    </TMPL_IF>

	    <input type="submit" value='Show timetable'>
	   </div>
	  </div>
	  <!-- box end //-->
	 </form>

	<!-- Ok, the user does NOT have the right to read the config of any host //-->
	<TMPL_ELSE>
	 <p>
	  You are not allowed to update the timetable of a host.
	 </p>
	</TMPL_IF>


	 <!-- If the user has the right to read the config of least on host //-->
	 <TMPL_IF timetable>
	 <form name="update_timetable_form" action="index.pl" method="POST">
	  <input type="hidden" name="page" value="UpdateTimetable">
<TMPL_VAR hidden_form_data>
	 <div class="box">
	  <div class="box_head">&raquo; Timetable of host <TMPL_VAR hostname></div>
	  <div class="box_content">
<TMPL_VAR timetable>
	   <input type="submit" value="Update timetable">
	  </div>
	 </div>
	 </form>

	  <div id='result'>
	   <TMPL_IF result>
	    <div class="box">
	     <div class="box_head">&raquo; Result</div>
	      <div class="box_content">
	       <TMPL_VAR result>
	      </div>
	     </div>
	   </TMPL_IF>
	  </div>
	 </TMPL_IF>
	</TMPL_IF>





	 <!-- If the user has the right to read the config of least on host //-->
	 <TMPL_IF timetable>
	 <form name="update_timetable_form" action="index.pl" MEthod="POST">
	  <input type="hidden" name="page" value="UpdateTimetable">
<TMPL_VAR hidden_form_data>
	 <div class="box">
	  <div class="box_head">&raquo; Zeitplan f&uuml;r Rechner <TMPL_VAR hostname></div>
	  <div class="box_content">
<TMPL_VAR timetable>

	   <input type="submit" value='Zeitplan speichern'>
	  </div>
	 </div>
	 </form>

	  <div id='result'>
	   <TMPL_IF result>
	    <div class="box">
	     <div class="box_head">&raquo; Ergebnis</div>
	      <div class="box_content">
	       <TMPL_VAR result>
	      </div>
	     </div>
	   </TMPL_IF>
	  </div>
	 </TMPL_IF>
	</TMPL_IF>
