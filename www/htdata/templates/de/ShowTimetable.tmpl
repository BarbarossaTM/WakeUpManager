	 <p>
	  Der WakeUpManager bietet die M&ouml;glichkeit, Rechner zeitgesteuert herauf- und herunterzufahren.
	 </p>

	 <p>
	  Um sich die Konfiguration eines Rechners anzeigen zu lassen, w&auml;hlen Sie
	  den gew&uuml;nschten PC aus der Liste aus und klicken Sie auf "Konfiguration anziegen".
	 </p>

	<!-- If the user has the right to read the config of least on host //-->
	<TMPL_IF host_loop>
	 <form name="show_config_form" action="" method="POST">

	  <p>
	   W&auml;hlen Sie den Rechner, dessen Zeitplan sie einsehen m&ouml;chten.
	  </p>

	  <div class="box">
	   <div class="box_head">&raquo; Zeitplan anzeigen</div>
	   <div class="box_content">

	    <div id='host_select'>
	     <select name="host">
	      <TMPL_LOOP NAME="host_loop"><option value="<TMPL_VAR NAME=key>"><TMPL_VAR NAME=val></option>
	      </TMPL_LOOP>
	     </select>
	    </div>

	    <!-- Nur Rechner der Gruppe <Hostgroup> anzeigen. //-->
	    <TMPL_IF hostgroup_loop>
	     <p class='hostgroup_list'>
	      Nur Rechner der Gruppe

	      <select name="group" onChange="updateHostlist();">
	       <TMPL_LOOP NAME="hostgroup_loop"><option value="<TMPL_VAR NAME=key>"><TMPL_VAR NAME=val></option>
	       </TMPL_LOOP>
	      </select>

	      anzeigen.
	     </p>
	    </TMPL_IF>

	    <input type="button" value='Zeitplan anzeigen' onClick="showConfig();">
	   </div>
	  </div>
	  <!-- box end //-->
	 </form>

	 <div id='result'>
	 </div>

	<!-- Ok, the user does NOT have the right to read the config of any host //-->
	<TMPL_ELSE>
	 <p>
	  Sie haben nicht das Recht den Zeitplan eines Rechners einzusehen.
	 </p>
	</TMPL_IF>
